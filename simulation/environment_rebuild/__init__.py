"""Environment rebuild line.

This package is the fresh simulation-environment line intended to replace the
legacy runtime over time.

It starts deliberately small:

* single runtime-owned physics scene
* explicit floor and robot contact materials
* programmatic actor spawn
* no forced dependency on legacy runtime behavior
"""

